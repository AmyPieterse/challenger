const db = require('../config');
//userId is primary key of users table
//userID is foreign key in orders table
//prodID is foreign key in orders table
//prodID is primary key in product table

//use using when they have the same name and on when they have the same name
class Orders{
    fetchOrders(req,res){
        
    }
    fetchOrder(req,res){

    }
    addOrder(req,res){

    }
    updateOrder(req,res){

    }
    deleteOrder(req,res){

    }
}
module.exports= Orders;